apiVersion: v1
kind: Service
metadata:
  name: {{ "{{ .Release.Name }}" }}-api
spec:
  type: NodePort
  selector:
    app: {{ "{{ .Release.Name }}" }}-api
  ports:
    - port: {{ "{{ .Values.api.port }}" }}
      nodePort: {{ "{{ .Values.api.nodePort }}" }}
